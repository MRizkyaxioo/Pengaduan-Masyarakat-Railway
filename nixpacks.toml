[phases.setup]
aptPkgs = [
  "php",
  "php-cli",
  "php-mysql",
  "php-xml",
  "php-mbstring",
  "php-zip",
  "php-gd",
  "php-curl",
  "php-bcmath",
  "php-intl"
]

[phases.build]
cmds = [
  "composer install --no-dev --optimize-autoloader",
  "npm install",
  "npm run build"
]

[phases.start]
cmds = [
  "php artisan migrate --force",
  "php artisan config:cache",
  "php artisan route:cache",
  "php artisan view:cache"
]

[start]
cmd = "php -d variables_order=EGPCS -S 0.0.0.0:8080 server.php"
